# sw - a simple stopwatch

## Installation

`make && make install`

## Usage

`sw`

`SPC` to pause/resume.

`q` to quit.

`sw -s` to save time to `~/.sw/savedtime`

`sw -r` to restore time from `~/.sw/savedtime`
